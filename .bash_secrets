# Do doppler stuff here
if [ -x $(which doppler) ] && [ $(cat ~/.doppler/.doppler.yaml | yq .scoped./.token) != "null" ]; then
    set -a
    source <(doppler secrets download --project jedrw --no-file --format env)
    set +a
fi
